
// Our Scraping tools

const
    axios = require('axios'),
    cheerio = require('cheerio');


let scrape = function (callback) {

    const articleArr = [];


    console.log(headlineObject);

    callback(headlineObject);
};

module.exports = scrape;
